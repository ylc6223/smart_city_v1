# application.yaml
spring:
  # 热部署
  devtools:
    restart:
      enabled: true  #设置开启热部署
      additional-paths: src/main/java #重启目录
      exclude: WEB-INF/**
    freemarker:
      cache: false    #页面不加载缓存，修改即时生效
    thymeleaf:
      encoding: utf-8
      # 禁用缓存
      cache: false